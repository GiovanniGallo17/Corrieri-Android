<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.unibas.corrieri" >

    <application
        android:name="it.unibas.corrieri.Applicazione"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="it.unibas.corrieri.activity.ActivityPrincipale"
            android:label="Corrieri Android" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="it.unibas.corrieri.activity.ActivityDettaglioCorriere"
            android:label="Corrieri Android"/>

        <activity android:name="it.unibas.corrieri.activity.ActivityNuovoPacco"
            android:label="Corrieri Android" />

        <activity android:name="it.unibas.corrieri.activity.ActivitySelezionaUtente"
            android:label="Corrieri Android" />


    </application>

</manifest>
